const LogStore=(()=>{const K="logbook_v2_logs";const getAll=()=>{try{return JSON.parse(localStorage.getItem(K)||"[]")}catch{return[]}};const saveAll=x=>localStorage.setItem(K,JSON.stringify(x));const add=x=>{const a=getAll();a.unshift(x);saveAll(a)};const update=(id,c)=>saveAll(getAll().map(x=>x.id===id?{...x,...c}:x));const remove=id=>saveAll(getAll().filter(x=>x.id!==id));const clear=()=>localStorage.removeItem(K);return{getAll,saveAll,add,update,remove,clear}})();