const SheetsSync=(()=>{const U="logbook_google_script_url_v2",Q="logbook_sync_queue_v2";const getUrl=()=>localStorage.getItem(U)||"";const setUrl=x=>localStorage.setItem(U,x.trim());const getQueue=()=>{try{return JSON.parse(localStorage.getItem(Q)||"[]")}catch{return[]}};const enqueue=x=>{const q=getQueue();if(!q.some(y=>y.id===x.id)){q.push(x);localStorage.setItem(Q,JSON.stringify(q))}};async function syncQueue(){const q=getQueue(),url=getUrl();if(!q.length||!url)return{synced:0};try{const r=await fetch(url,{method:"POST",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify({action:"sync",logs:q})}),d=await r.json();if(!r.ok||!d.ok)throw Error(d.error||"Sync failed");localStorage.setItem(Q,"[]");return{synced:q.length}}catch(error){return{synced:0,error}}}return{getUrl,setUrl,getQueue,enqueue,syncQueue}})();